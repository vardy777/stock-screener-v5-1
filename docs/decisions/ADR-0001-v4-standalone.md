# ADR-0001：V4独立运行

- 状态：已接受
- 日期：2026-08-06

## 决策

行情、账户、候选、推送、看板和模拟调度的实体实现全部位于 `v4/`。
`v4/` 禁止导入 `v3.*`；ChatGPT、Codex和Hermes不属于运行时依赖。

## 后果

历史V2可以保留用于审计；V3已生成可校验本地归档并移出工作树。通过自动测试扫描V3导入，
看板直接以 `python -m v4.dashboard` 启动。
