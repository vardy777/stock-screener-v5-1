# 真实窗口审计：2026-08-10 至 2026-08-11

## 2026-08-10 失败事实

- 09:25全市场快照落盘，但`morning_decision`直到09:36完成；09:25:20推送等待85秒后以`DEPENDENCY_NOT_SUCCEEDED`阻断。
- 原因是`snapshot_frame`在每一行重新计算全市场`MarketSnapshotV1.snapshot_id`，约4380只股票形成O(n²)哈希计算。
- 14:49 `feature_freeze`返回`EXIT_2`；冻结上下文只有114/4399行，覆盖2.59%，未达到95%。
- 14:50确认、推送、模拟买入、健康检查和维护随后被依赖门禁阻断。
- PushPlus没有被调用，因此用户没有收到昨日两次业务推送。

## 已实施修复

- 全市场DataFrame投影只计算一次快照ID。
- 行情新鲜度统一以不可变快照的`batch_completed_at`判断，不受下游计算耗时影响。
- 14:49失败作为终态仍允许14:50生成可审计的空仓/`paper_only`确认并推送。
- 健康检查失败不再阻断15:10下一交易日维护。

## 2026-08-11已验证

- 09:25快照：4378/4399，覆盖99.52%，新鲜覆盖99.52%，`data_valid=true`。
- 决策实体：`mp-fe7cadbdaa6264805697038d`，09:25:06落盘，任务成功。
- 市场模式：`risk_off`；上涨1062、下跌2735、平盘581。
- 候选为空：上一交易日冻结上下文未就绪，系统按门禁空池，没有使用未来数据或过期上下文。
- 09:25推送：PushPlus响应200，`ACCEPTED`，请求ID `665bf4b595d64e88a60e7f017b485697`。
- 09:30卖出任务成功；账户无持仓，因此是无成交成功回执，不构成真实卖出价格验收。

## 尚未通过

- 严格14:49特征冻结：上下文数据源仍需修复并达到95%覆盖。
- 14:50确认和PushPlus：等待2026-08-11真实窗口验证。
- P3真实买入与次交易日09:30成交：必须等实际产生合格模拟持仓后验证。

## 09:41 严格上下文恢复

- 新增独立的前一交易日上下文网关，使用腾讯前复权日线并发获取全市场历史数据，不再让受到 HTTP 456 限流的旧逐股归档刷新阻塞生产。
- 对 2026-08-11 的实际构建结果：4379/4399 行有效，覆盖率 99.55%；20 只股票因上一交易日数据缺失被隔离。
- 以上下游不可变 09:25 `MarketSnapshotV1.previous_close` 作为第二来源交叉校验：4359/4359 可比股票一致，匹配率 100%，容差 1%。
- 网关丢弃了 4377 条 2026-08-11 当日行，只允许使用截至 2026-08-10 的历史数据，未泄漏当日未来信息。
- 严格上下文已原子保存，`weekend_preflight` 判定 `strict_capture_ready=true`。这证明输入准备就绪，但不替代今天 14:49 真实窗口验收。

## 生产集成补强

- P2 14:50 买入行情和 P3 次日 09:30 卖出行情在通过 `MarketDataGateway` 后，直接投影到不可变严格执行快照归档；不再依赖已退役的旧采集定时任务。
- 当同日 09:25 母池为空时，买入快照健康检查明确记录 `NOT_APPLICABLE_EMPTY_MORNING_POOL`，避免将正确空仓误报成漏采。
- 修复 V3 退役后两个运维脚本仍引用不存在的 `v4.snapshots`；现统一引用 `v4.snapshot_compat`。
- 全量自动测试：234/234 通过。

## 14:49--15:20 真实窗口结果

- 14:49 特征任务按时运行并于 14:49:07 终止，真实快照 ID 为 `ms1-dc7ceed5858c9f83b7b113aa2cb80b3a96bc4eeb7c362144dc336e806e1e407c`。
- 该快照仅接受 3813/4379 只股票，覆盖率 87.07%，因此严格特征没有发布。系统没有补造或放宽这份失败证据。
- 根因是本机 Windows 时间服务未同步（`Local CMOS Clock`），本机比新浪供应商时间慢约 2 秒；`received_at < provider_time` 的约 566 行被因果契约正确隔离。当前普通权限无法执行 `w32tm /resync`，返回 `0x80070005`。
- 空早盘母池的尾盘实体此前错误使用无血缘的空市场状态，导致第一次确认失败。修复后于 14:53:30 生成空结实体 `cd-6a1b4c87b96b162c1f81cff3`，明确 `EMPTY/no_candidate`，沿用冻结的早盘快照血缘，没有伪造 14:50 行情。
- 下午 PushPlus 于 14:53:31 通过有界同日故障恢复发送，真实回执为 `200/ACCEPTED`，请求 ID `862b109e492347dea20442d14eca9a06`。这是延迟恢复成功，不算准点 14:50 验收通过。
- 空结模拟买入批次 `pebatch1-667604f18fa799d3c2f996d0` 已落盘：0 意图、0 成交、现金 100000 元、对账通过。它不构成真实买入成交验证。
- P2 验收已改为读取不可变通知回执，不再读取退役任务日志；今日早晚通知父实体 ID 均一致，P2 决策/通知链通过。
- 15:10 维护按时运行。次日上下文本身取得 4377/4399（99.50%），但因当天信号快照缺失 566 个参考价，双来源可比覆盖不足 95%，正确失败关闭。
- 新增独立新浪收盘参考适配器以补齐交叉验证；重复恢复请求触发腾讯临时限流。构建器已修复为未通过时绝不覆盖上一份合格上下文，仍需在供应商冷却后重新构建 2026-08-12 上下文。
- 修复后全量自动测试：236/236 通过。

## 16:30 次日上下文恢复

- 腾讯限流解除后仅启动一个 24 线程实例，成功构建 2026-08-12 所需上下文：4377/4399，覆盖率 99.50%。
- 独立新浪收盘参考可比 4375/4375，匹配率 100%；`strict_context_ready=true`，合格文件已原子发布。
- 修复维护幂等性：目标日期已有合格上下文时直接复用，不再重复请求全市场并触发限流。
- P4 维护第三次回执成功：`maintenance1-04ecf74d255ef869088c556a`；报告 `passed=true`、`existing_strict_context_reused=true`。
