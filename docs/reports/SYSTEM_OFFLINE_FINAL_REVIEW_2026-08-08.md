# V4全系统离线终审（2026-08-08）

## 结论

截至本报告，能够在非真实交易窗口内完成的软件实现、故障测试、架构检查和文档
校正已经完成。生产状态继续为`research_locked`。P1—P8均不能据此宣称生产完成：
剩余阻断项依赖真实交易窗口、自然积累的严格样本或单独的生产切换授权。

## 已验证事实

| 范围 | 离线终审结果 | 未执行事项 |
|---|---|---|
| P1 | 唯一行情网关、MarketSnapshotV1不可变存储、市场状态血缘、时间失败关闭 | 真实09:25/14:49/14:50/09:30 |
| P2 | candidate journal唯一事实源、最终实体投影、冻结快照全链回放 | 真实同日母池/确认ID链路 |
| P3 | 定点金额、事件链、并发、恢复、千次压力、旧账只读审计 | 生产模拟成交接入 |
| P4 | DAG、幂等、补偿、崩溃恢复、SLA、心跳、告警、通知哈希 | 改接生产任务及真实回执 |
| P5 | 只读读模型、零写入真实文件适配、12模块页面、多故障场景、响应式验收 | 8898原子切换 |
| P6 | 严格数据集、Walk-Forward和压力报告失败关闭审计 | 积累≥500笔严格样本并实际评估 |
| P7 | 模型/策略/训练说明/普通与压力报告发布包哈希终审 | 训练和发布合格模型 |
| P8 | 内容寻址备份、损坏检测、空目录隔离恢复 | 生产数据灾备演练及V2/V3归档 |

自动测试最终结果为221项通过、0项失败；项目一致性检查通过；V4运行代码没有
导入`v3.*`。PushPlus token没有出现在可跟踪文件中。

P5使用当前`v4/data`文件构建读模型时，候选、市场和资金流三个源文件构建前后
SHA-256完全不变。审计同时发现8月6—7日候选日志是缺少新版实体ID的旧格式，
现已显示`INVALID_ENTITY`而不是错误标记为完成。当前市场文件自身明确
`data_valid=false`，看板据此显示降级，不伪装为有效市场数据。

## 当前运行现场（只读检查）

- 8898当前由既有看板进程监听，P5没有改接；
- 本机可见9个`AStock-V4-*`任务处于Ready，包括09:25、14:49、14:50、
  09:30、健康巡检、维护和看板任务；本轮没有注册、修改或启用任何任务；
- 本轮没有发送PushPlus、没有写生产模拟账户、没有发布模型、没有归档历史代码、
  没有对生产数据执行备份或恢复。

## 周一开始必须取得的证据

1. 09:25：全市场版本化快照、市场状态、MorningPoolV1、推送请求与明确回执；
2. 14:49：严格特征冻结，时间、覆盖率、快照ID和feature context ID一致；
3. 14:50—14:51:59：ConfirmationDecisionV1严格属于当日母池，并保存严格与
   paper-only物理隔离快照；
4. 次交易日09:30—09:35：连续竞价卖出快照、下一开放交易日日历配对与执行结果；
5. 任一窗口失败：立即暂停P3—P8生产推进，优先修复P1/P2，不补造证据；
6. 持续自然积累严格样本；不足500笔或任一预定义统计门槛失败时不得训练发布模型。

## 保留风险

- 现有8898仍是旧生产看板，职责较重；已准备好P5替代实现，但切换需要真实实体
  观测验收和用户单独授权；
- P3/P4离线新实现尚未接入现有生产模拟成交和任务链，当前Ready任务不等于新链路
  已通过真实窗口；
- 严格14:50/09:30样本仍为0，代理历史结果不能用于盈利声明或生产准入；
- 历史8月6—7日候选日志只读保留旧格式，不能补写实体ID冒充当时已生成的新实体。
