# V5 离线复盘（2026-08-13）

## 已完成并验证

- V5 运行时代码不直接导入 `v4.*`；V4 历史事实保持只读。
- 数据采集、质量门禁、候选漏斗、09:25 母池与 14:50 子集确认契约已建立。
- V5 只读事实源只读取 `v5/data`，不读取 V4 候选日志、运行状态或看板派生文件。
- 本条为历史检查点：当时完成四页预览；随后已按用户要求重建为单页股票研究产品，当前事实以 `V5_DASHBOARD_ACCEPTANCE_2026-08-13.md` 为准。
- 桌面端和 390px 手机端完成浏览器视觉检查；空数据明确显示“不交易/证据不足”，不编造价格。
- `research_locked` 保持开启，券商订单保持禁用。

## 验收证据

- V5 专项测试：9 项通过。
- 全量测试：256 项通过，0 失败。
- 静态扫描：V5 运行时不存在直接 V4 导入。
- 切换预检正确拒绝生产切换：`real_source_ready=false`、`live_shadow_accepted=false`。
- V5 预览地址：`http://localhost:8899/`；V4 生产地址 8898 未被覆盖。

## 未完成且不能伪造

1. 东方财富独立实时全市场适配器和 V5 原生快照契约已经实现。首次探测 HTTP 200 并返回 5,549 条市场记录，后续请求被服务端主动断开；因此适配器完成，但稳定性和真实窗口 ≥95% 覆盖仍未验收，`real_source_ready` 继续为 false。
2. 09:24:30 与 14:48 预热、窗口内增量刷新、09:25/14:49/14:50/次日09:30 的真实影子验收尚未完成。
3. V5 独立影子调度、严格模拟往返和真实绩效事实尚无交易窗口证据。
4. 因上述门禁未通过，不能切换 V5 调度、账本、PushPlus 或 8898；V4 回滚基线继续保留。

## 结论

V5 的离线产品骨架和独立前端已经可验收，但实时产品闭环仍未达到生产切换标准。下一步必须接入第二实时源并通过真实影子窗口；通过后才能原子切换，不能以工期替代证据。
