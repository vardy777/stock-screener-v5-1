# V5 第二轮独立复验修复记录（2026-08-26）

## 边界

- 保持 `research_locked`，未接入券商订单。
- 未降低 95% 覆盖、匹配或一致性门槛。
- 未补写历史严格窗口，未调优冻结 baseline 的选股参数。

## 已修复

1. 最终买入对任一来源停牌、涨停锁定、无卖一或无卖一深度失败关闭；最终卖出对任一来源停牌、跌停锁定、无买一或无买一深度失败关闭。
2. 执行快照改为双源保守共识盘口：买入采用较高卖一价，卖出采用较低买一价，可成交数量采用双源深度最小值；同时保存两源盘口和共识规则。
3. baseline 冻结清单覆盖确认逻辑、数量规则、PaperEngine 最终现金流、买卖入口、佣金、最低佣金、印花税和滑点；`freeze`、`paper_sell` 统一先执行冻结校验。
4. 次日卖出配对使用执行快照完成时间和真实成交事件时间；双空仓日保存明确的 09:30 不可变观察事实。
5. 机会日和配对读取验证自身哈希、母池、确认、决策/执行快照、订单和事件血缘；伪造或篡改证据进入隔离，不能进入统计分母。
6. 五分组对 1 至 9 条小样本保证每条恰好进入一个连续等频组。
7. 无效证据返回 `EVIDENCE_INVALID` / `QUARANTINE`，不再解释为 challenger 的策略性 `KILL`。

## 验收事实

- 针对性反例：58 passed。
- 标准仓库根目录：478 passed。
- `scripts/offline_acceptance.py --run-tests`：passed；项目一致性、运行前检查、冻结回放、故障矩阵、秘密扫描、生产架构审计和测试均通过。
- `git diff --check`：通过。

## 仍待真实窗口验证

- 双独立行情源在真实 14:50 最终标的上的保守共识盘口及深度。
- 次日 09:30:00–09:30:59 卖出快照、baseline/challenger 成交事件与配对事实的完整血缘。
- 双空仓日真实 09:30 观察事实进入配对分母。
- 跨多个市场状态持续积累严格样本；这些结果用于验证策略，不代表策略已经有效或可以解除 `research_locked`。
