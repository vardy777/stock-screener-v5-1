# A股隔夜交易研究系统 V5

> 当前权威状态：V5 是唯一生产开发目标和运行主体；V4/V3 仅保留只读历史。系统保持 `research_locked`，不连接券商，策略有效性尚未得到严格样本证明。

这是项目的首要入口。任何新会话应依次阅读本文件、`docs/project-state.json`、`docs/ARCHITECTURE.md`、`docs/MODULES.md` 和最新审计报告。

## 产品目标

1. 08:30准备独立全市场股票池并检查时钟、日历和行情传输。
2. 09:25使用新浪与腾讯双源全市场快照形成可解释母池并推送。
3. 14:49冻结新双源快照；14:50只能在同日母池内确认并推送。
4. 14:50:40仅对确认Top1进行本地模拟买入；下一交易日09:30模拟卖出。
5. 自动记录费用、滑点、盘口深度、T+1、拒单、恢复、收益和可比Top1基线。
6. 在8899只读看板展示今日市场、候选、风险、模拟账户和证据结论。
7. 积累严格真实往返；达到样本、Walk-Forward和压力门禁后才允许发布模型。
8. 使用相同严格实时快照并行运行冻结基线与“量价挑战者”；挑战者叠加因果5/10日量价上下文、使用独立影子账本且不发送PushPlus。

## 当前生产事实

- 看板：[http://localhost:8899/](http://localhost:8899/)，入口 `python -m v5.dashboard`。
- 生产调度：11项 `AStock-V5-*-Daily` Windows循环任务，运行时再由交易日历和严格窗口门禁校验。
- 行情：东方财富市场目录形成股票池；新浪与腾讯形成独立报价共识。
- 全市场门槛：覆盖率不得低于95%；价格和响应时间一致率不得低于95%。
- 候选与决策、快照、市场状态、通知回执、模拟订单和成交均为V5不可变事实。
- 通知只有真实PushPlus `HTTP 200 / ACCEPTED`回执才算成功。
- V4受保护任务定义可能仍可见，但所有V4业务适配器在导入行情或执行代码前永久退出。
- V5是唯一模拟账本写者；不存在V4/V5双写。
- 基线是09:25/14:50 PushPlus唯一业务来源；`volume_price_v1`挑战者只在8899展示，失败不会阻断基线。

## 研究边界

- 不得补写已经错过的09:25、14:49、14:50或09:30严格窗口。
- 不得使用未来数据、15:00代理14:50或旧候选冒充当日事实。
- 午后恢复观察只能用于产品恢复诊断，明确不进入确认和模拟账户。
- 当前严格完整往返为0，规则因子和市场阈值仍是待验证基线，不代表上涨概率或盈利保证。

## 快速检查

```powershell
cd C:\Users\lisha\stock-screener
.\.venv\Scripts\python.exe -m pytest -q
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\audit_production_tasks.ps1
.\.venv\Scripts\python.exe -m v5.independence_audit
```

敏感配置仅位于被Git忽略的 `v5/.env`。不要在文档、测试输出或提交中记录Token。
