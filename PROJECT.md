# A股隔夜研究与模拟系统 V4

这是本项目的唯一入口。无论由用户、ChatGPT App、Codex 或其他开发工具
进入仓库，都应先阅读本文件和 [当前项目状态](docs/project-state.json)。

## 项目目标

建立一套本机独立、可审计、可测试、可持续演进的 A 股隔夜策略工具：

1. 工作日09:25生成重点观察母池并推送；
2. 14:50只在母池内确认候选并推送；
3. 对符合研究模拟契约的Top1自动模拟买入；
4. 下一交易日09:30连续竞价后自动模拟卖出；
5. 在8898看板记录每次决策、空仓原因、成交、费用、收益、胜率和证据质量；
6. 积累严格真实样本，达到准入标准后才训练、发布生产模型。

它不是盈利保证工具，当前也不是实盘下单系统。

## 当前结论

- 运行主体已经独立位于 `v4/`，不依赖 Hermes、Codex 或 `v3.*`。
- 生产状态必须保持 `research_locked`。
- 自动链路已能运行，但候选、决策、执行和证据口径仍需工程化统一。
- P0 文档存储已确认使用 UTF-8；Windows 终端输出编码已显式固定，阶段与架构说明已按代码事实校正。
- P1、P2离线工程已通过，P3/P4计划内离线验收已通过但仍受真实窗口与生产授权约束。P5只读控制台的离线实现与视觉验收已通过；P6严格数据审计、P7发布包审计和P8备份恢复已完成隔离离线契约。现有8898、生产调度、PushPlus、模拟成交生产链路和模型发布均未改接；P1/P2真实窗口失败时立即暂停后续阶段并优先修复。
- P3/P4/P5生产切换准备包已完成：四窗口自动验收、任务/单写者现场、切换和回滚顺序、冻结周期彩排及一键验收均为只读或隔离输出；缺少真实窗口和明确授权时不允许执行切换。

## 项目地图

| 入口 | 内容 |
|---|---|
| [项目状态](docs/project-state.json) | 机器可读的当前阶段、风险、下一任务和验证结果 |
| [路线图](docs/ROADMAP.md) | 按顺序推进的开发阶段与退出条件 |
| [详细日程](docs/PROJECT_PLAN.md) | 日期、任务、交付物、验收、稳定运行和样本时间 |
| [架构](docs/ARCHITECTURE.md) | 系统边界、数据流、状态机和证据隔离 |
| [模块清单](docs/MODULES.md) | 每个模块的职责、输入、输出、状态与待修事项 |
| [运行手册](docs/RUNBOOK.md) | 启动、检查、故障定位、恢复和每日操作 |
| [决策记录](docs/decisions/README.md) | 关键设计选择及原因 |
| [变更日志](docs/CHANGELOG.md) | 面向项目阶段的变更记录 |
| [V4运行说明](v4/README.md) | 当前V4代码和门禁语义 |

## 开发顺序

严格按照以下顺序，不跨阶段堆功能：

```text
项目基线 → 数据与时间 → 候选契约 → 决策门禁 → 模拟执行
        → 调度推送 → 看板观测 → 研究评估 → 模型发布
```

每个阶段都必须同时交付：模块契约、实现、测试、运行证据、文档和状态更新。

## 快速使用

```powershell
cd C:\Users\lisha\stock-screener
.\.venv\Scripts\python.exe scripts\project_status.py
.\.venv\Scripts\python.exe -m pytest -q
```

看板地址：[http://localhost:8898/](http://localhost:8898/)

敏感配置只存放在被 Git 忽略的 `v4/.env`，不得写入文档、日志或提交记录。
